apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agent-script.fullname" . }}-test-connection"
  labels:
    {{- include "agent-script.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agent-script.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
